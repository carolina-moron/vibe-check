// Where anonymised reports are sent. null = no backend connected: the form still builds and
// previews the anonymised record, but nothing leaves the browser.
// The endpoint must accept a JSON POST and must not log IP addresses.
export const REPORT_ENDPOINT = null;
